
import { Board, Widget } from "@/types";

// This file is kept for backward compatibility but boards are now created dynamically
// from the database for authenticated users
export const mockBoards: Board[] = [];
